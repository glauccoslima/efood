// jest-dom adiciona matchers personalizados do jest para fazer asserções em nós do DOM.
// Isso permite que você faça coisas como:
// expect(element).toHaveTextContent(/react/i)
// Saiba mais em: https://github.com/testing-library/jest-dom

// Importa a biblioteca '@testing-library/jest-dom' que estende as funcionalidades do jest
// com matchers personalizados para facilitar as asserções em testes de componentes React.
import '@testing-library/jest-dom'
